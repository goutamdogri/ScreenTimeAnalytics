{
  "name": "@screen-time/adapters",
  "version": "0.1.0",
  "private": true,
  "description": "PlatformAdapter implementations, used only by the desktop agent (Phase 2/6)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "jest"
  },
  "dependencies": {
    "dbus-next": "^0.10.2"
  },
  "devDependencies": {
    "@jest/globals": "^30.5.1",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.0",
    "jest": "^30.5.1",
    "ts-jest": "^29.4.12",
    "typescript": "~5.9.3"
  }
}
