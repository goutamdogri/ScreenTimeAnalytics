# Dependencies / build output — must be installed/Built fresh inside the image
**/node_modules
**/dist
**/coverage
**/.turbo
**/src/generated

# Secrets — never baked into images
.env
.env.*
!.env.example

# VCS and OS cruft
.git
.gitignore
*.log
.DS_Store

# Editor / tooling caches
.idea
.vscode